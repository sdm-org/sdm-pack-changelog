subscription CommitWithChangelog {
  Commit {
    sha
    pushes {
      branch
    }
    message @matches(re: "(?s).*\\[changelog:.*\\].*") @required
    repo {
      owner
      name
      org {
        provider {
          url
        }
      }
    }
  }
}

